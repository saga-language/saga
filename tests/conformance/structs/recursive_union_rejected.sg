// A union stores its alternatives inline too — its payload is as wide as the
// widest one — so routing the cycle through it does not bound the size.
import "std/io"

struct Node {
  value int
  tail Node | Missing
}

pub fn Main() void {
  n := Node{value: 1}
  io.Println("{n.value}")
}
