// Spec: optional field via `Type | Missing` resolved with `or`.
// (docs/language.md:833-846)
import "std/io"

struct Payload {
  optional string | Missing
}

pub fn Main() void {
  p := Payload{optional: Missing{}}
  v := p.optional or { "unknown" }
  io.Println(v)
}
