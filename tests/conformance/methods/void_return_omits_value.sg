// Spec: "If a method has a return type of `Void`, then the `return`
// must omit a value." (docs/language.md:192-193)
import "std/io"

fn Logged(s string) void {
  if s == "skip" {
    return
  }
  io.Println(s)
}

pub fn Main() void {
  Logged("hi")
  Logged("skip")
  Logged("bye")
}
