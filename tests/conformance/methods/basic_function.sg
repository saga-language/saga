// Spec: a private function with single return value, tail-expression
// return. (docs/language.md:200, "fn Add(a, b Int) Int { a + b }")
import "std/io"

fn Add(a, b int) int { a + b }

pub fn Main() void {
  io.Println(Add(2, 3).String())
}
