// Spec: `next` advances to the next iteration without running the
// rest of the body. (docs/language.md:1200-1201)
import "std/io"

pub fn Main() Void {
  for v : [1, 2, 3, 4] {
    if v == 2 { next }
    io.Println(v.String())
  }
}
