// Spec: typed array declaration with initializer.
// (docs/language.md:602, "arr2 [Int] = [] // valid, type is known")
import "std/io"

pub fn Main() Void {
  arr [Int] = [1, 2, 3]
  io.Println(arr.Size().String())
}
