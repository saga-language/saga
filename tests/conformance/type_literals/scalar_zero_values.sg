// Spec: bare typed declaration uses the type's zero value.
// (docs/language.md:581-593)
import "std/io"

pub fn Main() void {
  b bool
  i int
  f float
  s string
  io.Println(b.String())
  io.Println(i.String())
  io.Println(f.String())
  io.Println("[" + s + "]")
}
