// Spec cross-cutting: explicit-typed declaration with initializer for a
// scalar type. The matching array form is `arr Int[] = []`
// (see docs/language.md).
import "std/io"

pub fn Main() void {
  x int = 5
  io.Println(x.String())
}
