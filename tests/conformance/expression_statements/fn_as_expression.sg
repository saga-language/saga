// Spec: fn can be used as an expression — a closure literal that can
// be assigned to a variable and called. (docs/language.md:154-156,
// line 1015 ff. for closures.)
import "std/io"

pub fn Main() void {
  add1 := fn(x int) int { x + 1 }
  io.Println(add1(5).String())
}
