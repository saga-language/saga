// `void` is the absence of a value, so nothing can hold one: not a variable,
// not a parameter, not a constant, not a field. Its legal uses are a return
// type and a union alternative (`T | void`).
const io = import "std/io"

pub fn Main() void {
  x void = null
  _ := x
  io.Println("ok")
}
