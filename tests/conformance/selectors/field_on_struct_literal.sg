// Spec: dot-access on a struct literal returns the field value.
// (docs/language.md:1067-1068)
import "std/io"

struct Box {
  value int
}

pub fn Main() void {
  i := Box{value: 42}.value
  io.Println(i.String())
}
