// Spec: a constant array is a compile-time value baked into read-only data;
// reading it allocates nothing. (docs/language.md "Compile-time values")
import "std/io"

const Primes = [2, 3, 5, 7]

pub fn Main() void {
  io.Println(Primes.Size().String())
}
