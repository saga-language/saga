// Spec: `if value == Int` narrows a union type within the branch.
// (docs/language.md:1144-1150)
import "std/io"

fn Describe(v int | float) string {
  if v is int {
    "int " + v.String()
  } else {
    "float " + v.String()
  }
}

pub fn Main() void {
  io.Println(Describe(7))
}
