// A constant is immutable: an in-place mutator (`Pop`) must be rejected.
// Value-returning methods like `Append` are allowed — see
// constants/const_array_append_exchange.
import "std/io"

const Primes = [2, 3, 5]

pub fn Main() void {
  last := Primes.Pop()
  io.Println(last.String())
}
