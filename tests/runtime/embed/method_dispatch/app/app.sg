import "std/io"

struct Greeter {
  prefix string
}

fn (g Greeter) Greet(name string) string {
  "{g.prefix} {name}"
}

struct U {
  Greeter
  who string
}

pub fn Main() void {
  u := U{who: "world", prefix: "hello"}
  io.Println(u.Greet(u.who))
}
