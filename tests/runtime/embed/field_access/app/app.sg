import "std/io"

struct Timestamps {
  created int
  updated int
}

struct User {
  Timestamps
  name string
}

pub fn Main() void {
  u := User{name: "alice", created: 1, updated: 2}
  io.Println(u.name)
  io.Println("{u.created}")
  io.Println("{u.updated}")
}
