import "std/io"
import "lib"

struct User {
  lib.Timestamps
  name string
}

pub fn Main() void {
  u := User{name: "alice", created: 1, updated: 2}
  io.Println(u.name)
  io.Println("{u.created}")
  io.Println("{u.updated}")
}
